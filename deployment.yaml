apiVersion: v1
kind: Service
metadata:
  name: helloworld
  namespace: hello
  labels:
    app: helloworld
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloworld
  namespace: hello

spec:
  replicas: 2
  selector:
    matchLabels:
      app: helloworld
  template:
    metadata:
      labels:
        app: helloworld
    spec:
      containers:
        - name: helloworld
          image: mustafaeid/test:test
          ports:
            - containerPort: 8080


